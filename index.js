const start = () => {
    while (Option) {
        let option = "out"
        switch (option) {
            case "cadastrar":
                break;
            case "listar":
                break;
            case "sair":
                break;
        }

    }
}